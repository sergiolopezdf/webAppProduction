kind: Service
apiVersion: v1
metadata:
  name: networkingservice
spec:
  # type: NodePort
  selector:
    app: webapp
  ports:  
  - name: http
    protocol: TCP
    port: 80
    # nodePort: 30001
    targetPort: 3000
  externalIPs:
  - 192.168.99.100
